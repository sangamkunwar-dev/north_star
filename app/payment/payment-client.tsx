'use client'

import { useState } from 'react'
import { ArrowLeft, Check, CreditCard, QrCode, ShieldCheck } from 'lucide-react'

const khaltiQr = 'https://hebbkx1anhila5yf.public.blob.vercel-storage.com/image-vReucrepROLzoZ8fKDALzfz4ZGZ2WG.png'

export default function PaymentClient({ plan }: { plan: 'creator' | 'studio' }) {
  const [method, setMethod] = useState<'card' | 'qr'>('card')
  const [submitted, setSubmitted] = useState(false)
  const [error, setError] = useState('')
  const name = plan === 'studio' ? 'Studio' : 'Creator'
  const price = plan === 'studio' ? 'रु 2,499' : 'रु 999'

  function submit(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault()
    setError('')
    setSubmitted(true)
  }

  if (submitted) return <main className="grid min-h-screen place-items-center bg-background px-6 py-12 text-foreground"><div className="w-full max-w-lg rounded-3xl border border-border bg-card p-8 text-center shadow-xl"><div className="mx-auto grid size-14 place-items-center rounded-full bg-primary/10 text-primary"><Check /></div><p className="mt-6 text-sm font-bold uppercase tracking-widest text-primary">Payment submitted</p><h1 className="mt-2 text-3xl font-semibold">{name} review started</h1><p className="mt-4 text-sm leading-6 text-muted-foreground">Your payment request is pending admin approval. We will review it within 24 hours. Your plan and paid features activate only after approval.</p><a href="/" className="mt-7 inline-flex items-center gap-2 rounded-xl bg-primary px-5 py-3 text-sm font-bold text-primary-foreground"><ArrowLeft data-icon="inline-start" />Return to dashboard</a></div></main>

  return <main className="min-h-screen bg-background px-4 py-8 text-foreground sm:px-6 sm:py-12"><div className="mx-auto max-w-5xl"><a href="/" className="inline-flex items-center gap-2 text-sm font-semibold text-muted-foreground hover:text-foreground"><ArrowLeft data-icon="inline-start" />Back to Plans & billing</a><div className="mt-8 grid gap-6 lg:grid-cols-[0.8fr_1.2fr]"><section className="rounded-3xl border border-border bg-card p-6 sm:p-8"><p className="text-sm font-bold uppercase tracking-widest text-primary">Secure checkout</p><h1 className="mt-3 text-3xl font-semibold">Subscribe to {name}</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">Submit your payment for manual verification. Your subscription is not active until an admin accepts it.</p><div className="mt-8 rounded-2xl bg-muted p-5"><p className="text-xs font-bold uppercase tracking-widest text-muted-foreground">Selected plan</p><div className="mt-3 flex items-end justify-between gap-4"><p className="text-xl font-semibold">{name}</p><p className="text-2xl font-bold text-primary">{price}<span className="text-sm font-medium text-muted-foreground"> / month</span></p></div></div><div className="mt-6 flex items-start gap-3 text-sm text-muted-foreground"><ShieldCheck className="mt-0.5 size-5 shrink-0 text-primary" /><p>Admin review protects your account. Do not submit sensitive card data outside a secure provider checkout.</p></div></section><section className="rounded-3xl border border-border bg-card p-6 sm:p-8"><div className="flex gap-3"><button type="button" onClick={() => setMethod('card')} className={`flex flex-1 items-center justify-center gap-2 rounded-xl px-4 py-3 text-sm font-bold ${method === 'card' ? 'bg-primary text-primary-foreground' : 'border border-border text-muted-foreground'}`}><CreditCard data-icon="inline-start" />Card</button><button type="button" onClick={() => setMethod('qr')} className={`flex flex-1 items-center justify-center gap-2 rounded-xl px-4 py-3 text-sm font-bold ${method === 'qr' ? 'bg-primary text-primary-foreground' : 'border border-border text-muted-foreground'}`}><QrCode data-icon="inline-start" />Khalti QR</button></div>{method === 'qr' ? <form onSubmit={submit} className="mt-8 text-center"><img src={khaltiQr} alt="Khalti payment QR code" className="mx-auto size-64 rounded-2xl border border-border object-contain" /><p className="mt-4 text-sm font-semibold">Scan with the Khalti app</p><p className="mt-1 text-xs text-muted-foreground">After payment, submit the request for admin verification.</p><button type="submit" className="mt-6 w-full rounded-xl bg-primary px-5 py-3 text-sm font-bold text-primary-foreground">I have paid — submit for review</button></form> : <form onSubmit={submit} className="mt-8"><div className="flex flex-col gap-4"><label className="text-sm font-semibold">Cardholder name<input required autoComplete="cc-name" className="field mt-2" placeholder="Name on card" /></label><label className="text-sm font-semibold">Card number<input required inputMode="numeric" autoComplete="cc-number" className="field mt-2" placeholder="Enter card number" /></label><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold">MM / YY<input required autoComplete="cc-exp" className="field mt-2" placeholder="MM / YY" /></label><label className="text-sm font-semibold">CVV<input required inputMode="numeric" autoComplete="cc-csc" className="field mt-2" placeholder="CVV" /></label></div></div>{error && <p role="alert" className="mt-3 text-sm text-destructive">{error}</p>}<button type="submit" className="mt-6 w-full rounded-xl bg-primary px-5 py-3 text-sm font-bold text-primary-foreground">Submit {price} payment for review</button><p className="mt-3 text-center text-xs text-muted-foreground">Demo request only. Raw card details are not stored.</p></form>}</section></div></div></main>
}
